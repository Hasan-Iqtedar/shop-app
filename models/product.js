class Product {
    constructor(productId, userId, title, imageURL, description, price) {
        this.productId = productId;
        this.userId = userId;
        this.title = title;
        this.imageURL = imageURL;
        this.description = description;
        this.price = price;
    }
}

export default Product;